export default  SaveComponent{
    constructor(saveSection, savedArticles, fireBaseRef){
        this.saveSection = saveSection;
        this.savedArticles = savedArticles;
        this.fireBaseRef = fireBaseRef;
    }
}